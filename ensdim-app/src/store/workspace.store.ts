"use client";

// Workspace data is now fetched from Supabase via profiles.workspace_id.
// Use useWorkspaceId() from auth.store for the workspace ID.
// Use Supabase queries to fetch workspace details when needed.

export {};
